// for in
let person = {
   name: 'Veda',
   age: 3
}
for(let key in person)
console.log(key, person[key]);


const colors = ['black', 'white', 'green','orange'];
for(let index in colors)
console.log(index, colors[index]);